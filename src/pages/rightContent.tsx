import styles from './index.less';

export default function RightContent() {
  return (
    <div className={styles.RightContent}>
      <h1 className={styles.title}>RightContent</h1>
    </div>
  );
}
